NODE_ENV=development
API_PORT=4000
# Preferred for Supabase projects (session pooler or direct connection string):
SUPABASE_DB_URL=
# Fallback for local/docker or non-Supabase Postgres:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/production_line
# Optional DB tuning:
DB_SSL=
DB_SSL_REJECT_UNAUTHORIZED=true
DB_POOL_MAX=10
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=10000
DB_QUERY_TIMEOUT_MS=15000
DB_STATEMENT_TIMEOUT_MS=20000
DB_MAX_RETRIES=1
DB_RETRY_DELAY_MS=250
HTTP_REQUEST_TIMEOUT_MS=30000
HTTP_HEADERS_TIMEOUT_MS=35000
HTTP_KEEP_ALIVE_TIMEOUT_MS=5000
# Use a random 32+ character secret.
JWT_SECRET=replace-with-a-32-plus-char-random-secret
JWT_EXPIRES_IN=12h
FRONTEND_ORIGIN=http://localhost:8080
